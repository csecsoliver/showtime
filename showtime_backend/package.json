{
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "nitropack": "latest"
  },
  "dependencies": {
    "bcrypt": "^6.0.0"
  }
}
